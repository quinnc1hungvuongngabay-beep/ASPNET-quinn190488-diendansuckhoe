@model List<HealthForumMVC.Models.Post>
@{
    ViewData["Title"] = "Trang chủ";
    var stats = ViewBag.Stats;
}

<div class="p-6 space-y-8">
    <!-- Welcome Section -->
    <div class="bg-gradient-to-r from-blue-600 to-blue-700 text-white rounded-xl p-8">
        <div class="max-w-3xl">
            <h1 class="text-3xl font-bold mb-4">
                @if (ViewBag.Category != null)
                {
                    <span>📊 @ViewBag.Category.Name</span>
                }
                else if (ViewBag.Filter == "popular")
                {
                    <span>🔥 Bài viết phổ biến</span>
                }
                else if (ViewBag.Filter == "trending")
                {
                    <span>⬆️ Bài viết thịnh hành</span>
                }
                else if (ViewBag.Filter == "saved")
                {
                    <span>🔖 Bài viết đã lưu</span>
                }
                else
                {
                    <span>Chào mừng đến với HealthForum 🏥</span>
                }
            </h1>
            <p class="text-blue-100 text-lg mb-6">
                Nơi chia sẻ kiến thức, kinh nghiệm và tìm kiếm lời khuyên về sức khỏe từ cộng đồng
            </p>
            <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                <div class="bg-white/10 rounded-lg p-4 text-center">
                    <i data-lucide="users" class="h-6 w-6 mx-auto mb-2 text-blue-200"></i>
                    <div class="text-xl font-bold">@stats.MemberCount</div>
                    <div class="text-sm text-blue-200">Thành viên</div>
                </div>
                <div class="bg-white/10 rounded-lg p-4 text-center">
                    <i data-lucide="message-circle" class="h-6 w-6 mx-auto mb-2 text-blue-200"></i>
                    <div class="text-xl font-bold">@stats.PostCount</div>
                    <div class="text-sm text-blue-200">Bài viết</div>
                </div>
                <div class="bg-white/10 rounded-lg p-4 text-center">
                    <i data-lucide="clock" class="h-6 w-6 mx-auto mb-2 text-blue-200"></i>
                    <div class="text-xl font-bold">@stats.OnlineCount</div>
                    <div class="text-sm text-blue-200">Online</div>
                </div>
                <div class="bg-white/10 rounded-lg p-4 text-center">
                    <i data-lucide="trending-up" class="h-6 w-6 mx-auto mb-2 text-blue-200"></i>
                    <div class="text-xl font-bold">@stats.ActivityRate</div>
                    <div class="text-sm text-blue-200">Hoạt động</div>
                </div>
            </div>
        </div>
    </div>

    <!-- Filter Tabs -->
    <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-1">
        <div class="flex space-x-1">
            <a href="/" class="px-4 py-2 rounded-md text-sm font-medium @(ViewBag.Filter == null ? "bg-blue-600 text-white" : "text-gray-600 hover:text-gray-900 hover:bg-gray-100")">
                <i data-lucide="clock" class="h-4 w-4 inline-block mr-2"></i>
                Mới nhất
            </a>
            <a href="/Home/Popular" class="px-4 py-2 rounded-md text-sm font-medium @(ViewBag.Filter == "popular" ? "bg-blue-600 text-white" : "text-gray-600 hover:text-gray-900 hover:bg-gray-100")">
                <i data-lucide="trending-up" class="h-4 w-4 inline-block mr-2"></i>
                Phổ biến
            </a>
            <a href="/Home/Trending" class="px-4 py-2 rounded-md text-sm font-medium @(ViewBag.Filter == "trending" ? "bg-blue-600 text-white" : "text-gray-600 hover:text-gray-900 hover:bg-gray-100")">
                <i data-lucide="trending-up" class="h-4 w-4 inline-block mr-2"></i>
                Thịnh hành
            </a>
        </div>
    </div>

    <!-- Posts Feed -->
    <div class="space-y-6">
        @if (Model.Any())
        {
            @foreach (var post in Model)
            {
                <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 hover:shadow-md transition-shadow">
                    <!-- Post Header -->
                    <div class="flex items-start justify-between mb-4">
                        <div class="flex items-center space-x-3">
                            <div class="w-10 h-10 bg-blue-600 rounded-full flex items-center justify-center">
                                <span class="text-white font-semibold">@post.Author.FullName.Substring(0, 1)</span>
                            </div>
                            <div>
                                <div class="flex items-center space-x-2">
                                    <h4 class="font-semibold text-gray-900">@post.Author.FullName</h4>
                                    @if (post.Author.Role == HealthForumMVC.Models.UserRole.Admin)
                                    {
                                        <span class="bg-red-100 text-red-800 text-xs px-2 py-1 rounded-full">Admin</span>
                                    }
                                </div>
                                <div class="flex items-center space-x-2 text-sm text-gray-500">
                                    <span>@@@post.Author.UserName</span>
                                    <span>•</span>
                                    <span>@post.CreatedAt.ToString("dd/MM/yyyy HH:mm")</span>
                                    <span>•</span>
                                    <span class="bg-blue-100 text-blue-800 px-2 py-1 rounded-full text-xs">
                                        @post.Category.Name
                                    </span>
                                </div>
                            </div>
                        </div>
                        @if (post.IsPinned)
                        {
                            <i data-lucide="pin" class="h-5 w-5 text-blue-600"></i>
                        }
                    </div>

                    <!-- Post Content -->
                    <div class="mb-4">
                        <h2 class="text-xl font-bold text-gray-900 mb-2 hover:text-blue-600 cursor-pointer">
                            <a href="/Home/PostDetail/@post.Id">@post.Title</a>
                        </h2>
                        <p class="text-gray-700 line-clamp-3">@post.Content</p>
                    </div>

                    <!-- Post Stats -->
                    <div class="flex items-center justify-between pt-4 border-t border-gray-100">
                        <div class="flex items-center space-x-6 text-sm text-gray-500">
                            <div class="flex items-center space-x-1">
                                <i data-lucide="eye" class="h-4 w-4"></i>
                                <span>@post.ViewCount</span>
                            </div>
                            <div class="flex items-center space-x-1">
                                <i data-lucide="heart" class="h-4 w-4"></i>
                                <span>@post.LikeCount</span>
                            </div>
                            <div class="flex items-center space-x-1">
                                <i data-lucide="message-circle" class="h-4 w-4"></i>
                                <span>@post.CommentCount</span>
                            </div>
                        </div>
                        <a href="/Home/PostDetail/@post.Id" class="text-blue-600 hover:text-blue-800 text-sm font-medium">
                            Đọc thêm →
                        </a>
                    </div>
                </div>
            }
        }
        else
        {
            <div class="text-center py-12">
                <i data-lucide="message-circle" class="h-16 w-16 text-gray-300 mx-auto mb-4"></i>
                <h3 class="text-lg font-medium text-gray-900 mb-2">
                    Chưa có bài viết nào
                </h3>
                <p class="text-gray-600">
                    Hãy là người đầu tiên chia sẻ câu chuyện của bạn!
                </p>
            </div>
        }
    </div>

    <!-- Load More -->
    @if (Model.Any())
    {
        <div class="text-center">
            <button class="bg-white border border-gray-300 text-gray-700 px-6 py-3 rounded-lg hover:bg-gray-50 transition-colors">
                Xem thêm bài viết
            </button>
        </div>
    }
</div>

<script>
    lucide.createIcons();
</script>